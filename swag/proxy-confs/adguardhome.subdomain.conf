## Version 2025/08/28
# make sure that your adguardhome container is named adguardhome
# make sure that your dns has a cname set for adguardhome

server {
    listen 443 ssl;
#    listen 443 quic;
    listen [::]:443 ssl;
#    listen [::]:443 quic;

    server_name adguardhome.*;

    include /config/nginx/ssl.conf;

    client_max_body_size 0;

    # enable for ldap auth (requires ldap-location.conf in the location block)
    #include /config/nginx/ldap-server.conf;

    # enable for Authelia (requires authelia-location.conf in the location block)
    #include /config/nginx/authelia-server.conf;

    # enable for Authentik (requires authentik-location.conf in the location block)
    #include /config/nginx/authentik-server.conf;

    # The one door. Server half; the location half is inside `location /`.
    # No path-scoped exemption here, unlike the *arr confs: AdGuard's entire
    # HTTP surface is its admin UI, and its /control/* API is the same surface
    # by another name -- it changes filtering for every device in the house.
    # Nothing outside this box consumes it. ADR-0034, ADR-0043.
    include /config/nginx/tinyauth-server.conf;

    location / {
        # enable the next two lines for http auth
        #auth_basic "Restricted";
        #auth_basic_user_file /config/nginx/.htpasswd;

        # enable for ldap auth (requires ldap-server.conf in the server block)
        #include /config/nginx/ldap-location.conf;

        # enable for Authelia (requires authelia-server.conf in the server block)
        #include /config/nginx/authelia-location.conf;

        # enable for Authentik (requires authentik-server.conf in the server block)
        #include /config/nginx/authentik-location.conf;

        include /config/nginx/tinyauth-location.conf;

        include /config/nginx/proxy.conf;
        include /config/nginx/resolver.conf;
        set $upstream_app adguardhome;
        # 3000 is the container-internal admin port and must STAY 3000. The
        # setup wizard offers to move the admin UI (its own suggestion is 80),
        # and taking that offer breaks both this proxy_pass and the container's
        # healthcheck, which probes 3000 too. ADR-0043.
        set $upstream_port 3000;
        set $upstream_proto http;
        proxy_pass $upstream_proto://$upstream_app:$upstream_port;

    }
}
